import React from "react";

const Header = () => {
    return (
        <div>
            <a href="#">Home</a>
            <a href="#">Contacts</a>
            <a href="#">Filter</a>
        </div>
    );
}

export default Header;